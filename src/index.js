import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

const name = 'tamibouz';

ReactDOM.render(
  <App name={name} />,
  document.getElementById('app')
);
